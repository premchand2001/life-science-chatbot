# Virtual environment
.venv/

# Environment variables (keep API keys safe!)
.env

# Python cache
__pycache__/
*.pyc

# Database (local only)
chatbot_memory.db

# Chroma DB (will rebuild on server)
chroma_db/

# Uploaded PDFs (local only)
uploaded_pdfs/